server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/
    username: root
    password:

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.platao.mybatis.model

pagehelper:
  helper-dialect: mysql
  reasonable: true