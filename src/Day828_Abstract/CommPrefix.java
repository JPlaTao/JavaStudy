package Day828_Abstract;

public class CommPrefix {
    public String commPrefix(String... strings){
        String commPrefix = "";
        for (int i = 0; i < strings.length; i++) {
            char c = strings[0].charAt(i);

        }
        return commPrefix;
    }
}
